scaf - simple auto cpu frequencer
=================================
Work In Porgress (ONLY FOR TESTING)

sacf it's a daemon that monitors the cpu N number of seconds and simply tweak
governors and decided whether to use turbo boost or not.

Usage
-----
With no arguments it runs as daemon (currently the daemon does nothing)

	sacf [-cvltT]


TODO
====
- freebsd and openbsd support
- man page
- variable (config.h) about how many seconds too 'sleep' before doing the 'frequencer' part
- variable (config.h) whether to have turbo boost always on.
